import { Harness } from "./harness";

// Superficie de prototipo de la skill `/prototype` — cuatro direcciones para el
// bloque de formulario de la futura `/contacto` (P66). Se borra al promover el
// ganador. Nada de producción importa de aquí.

export default function ContactoPrototipos() {
  return <Harness />;
}
